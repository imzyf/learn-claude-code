# s04 环境变量的默认值参考，程序不加载这份文件。
# 要改某一项：在命令行内联，只对这一次运行生效，见 README.md。

# 额外注册一个一次性 Stop hook，让循环续一轮（registerDefaultHooks）。
# 默认不设置：只有 README.md 第 6 节要看这个场景时才设成 1。
# 判断方式是变量存不存在，任何非空值都算开。
# S04_FORCE_STOP_HOOK=1
