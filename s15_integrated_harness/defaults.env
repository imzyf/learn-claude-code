# s15 环境变量的默认值参考，程序不加载这份文件。
# 要改某一项：在命令行内联，只对这一次运行生效，见 manual-check.md。

# 主模型连续两次 529 之后切过去的备用模型（MAX_CONSECUTIVE_529）。
# 默认空字符串：没配就一直用主模型重试，不切模型。
# FALLBACK_MODEL_ID=

# 其余重试和恢复的上限写死在 main.ts 里，不读环境变量：
# ESCALATED_MAX_TOKENS=16000 MAX_RETRIES=3 MAX_CONSECUTIVE_529=2 MAX_RECOVERY_RETRIES=2 BASE_DELAY_MS=500
