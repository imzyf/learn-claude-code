{
  "name": "learn-claude-code",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript port of learn-claude-code, built on the Vercel AI SDK",
  "scripts": {
    "dev": "tsx --env-file-if-exists=.env",
    "smoke": "tsx --env-file-if-exists=.env lib/smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.8",
    "ai": "^7.0.15",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
